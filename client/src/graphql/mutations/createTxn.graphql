mutation createTransaction($options: TransactionInput!) {
  createTransaction(options: $options) {
    id
    amount
    category {
      id
    }
    currency
    details
    isDiscretionary
    title
    txnDate
    type
    createdAt
    updatedAt
  }
}
